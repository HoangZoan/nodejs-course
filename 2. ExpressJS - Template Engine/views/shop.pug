extends layouts/main-layout.pug

block styles 
    link(rel="stylesheet", href="/css/main.css")

block content
    main
        if prods.length > 0
            div 
                each product in prods
                    article 
                        h1 #{product.title}
                        button Add to Cart
        else 
            h1 No Products
                    